{
  "name": "testing",
  "private": true,
  "scripts": {
    "build": "cd packages/db && npm run prisma:generate && npm run prisma:migrate && turbo build",
    "dev": "turbo dev",
    "websocket": "cd apps/websocket-server && npm run build && npm run start",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "postinstall": "cd packages/db && npm run prisma:generate && mkdir -p node_modules && cp -R ../../node_modules/.prisma node_modules/.prisma"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prisma": "^5.17.0",
    "turbo": "^2.0.7",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.8.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "@prisma/nextjs-monorepo-workaround-plugin": "^5.17.0",
    "pusher": "^5.2.0",
    "pusher-js": "^8.4.0-rc2"
  }
}
